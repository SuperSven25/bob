var prompt = require('prompt-sync')();
var ps = (prompt);

let names = [ "waterbending", "earthbending", "windbending", "firebending"  ]

names.push("the avatar can master them all")


for (let i = 0; i<names.length; i++){
    console.log(names[i])
}

/*
Length gör att coden skriver ut hur många element som finns i arrayn.
*/
